# Email for Let's Encrypt notifications (cert expiry warnings)
ACME_EMAIL=admin@kfzblitz24-group.com

# Traefik Dashboard Basic Auth
# Generate with: htpasswd -nb admin yourpassword
# Then escape $ as $$ for docker-compose
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$xxxxx$$xxxxxxxxxxxxxxxxxxxxxx
