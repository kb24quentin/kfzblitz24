package de.kfzblitz24.retoure_pda.ui.theme

import androidx.compose.ui.graphics.Color

// ── kfzBlitz24 Brand-Farben (aus CLAUDE.md + Design-Guide) ──────────────────
val Navy       = Color(0xFF0B3756)   // Primär / Header-Hintergrund
val Orange     = Color(0xFFFF6600)   // Akzent / CTA / aktiver Schritt
val LightGrey  = Color(0xFFE6E8EB)   // Card-Hintergrund (Light)
val MidGrey    = Color(0xFF8A93A0)   // Sekundärer Text
val DarkGrey   = Color(0xFF3D4654)   // Card-Surface im Dark-Theme

// ── Semantisch ───────────────────────────────────────────────────────────────
val VerdictGreen  = Color(0xFF4CAF50)
val VerdictYellow = Color(0xFFFFC107)
val VerdictRed    = Color(0xFFF44336)

val StepDone      = Color(0xFF2E7D32)   // grüner Schritt-Indikator (done)
val StepInactive  = Color(0x1AFFFFFF)   // weißlich-transparent für inaktive Steps

val SurfaceDark   = Color(0xFF0D1B2A)   // Haupt-Hintergrund (dunkler als Navy)
val CardDark      = Color(0x1AFFFFFF)   // weiße Glasmorphism-Cards
val CardBorder    = Color(0x1AFFFFFF)   // Kanten der Cards

val White80       = Color(0xCCFFFFFF)
val White60       = Color(0x99FFFFFF)
val White40       = Color(0x66FFFFFF)
