# Database
DB_PASSWORD=             # generate strong random (e.g. openssl rand -base64 24)

# NextAuth
AUTH_SECRET=             # generate: openssl rand -base64 32

# Resend (transactional email)
RESEND_API_KEY=          # from resend.com dashboard
FROM_EMAIL=corinna.wagner@kfzblitz24-group.com   # must be on a verified domain in Resend
FROM_NAME=Corinna Wagner - kfzBlitz24            # display name shown to recipients

# First admin user (auto-created via seed on first deploy)
ADMIN_EMAIL=info@kfzblitz24.de
ADMIN_PASSWORD=          # set on first deploy; remove after user is created
ADMIN_NAME=Admin
