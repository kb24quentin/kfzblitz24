services:
  crm-db:
    image: postgres:16-alpine
    container_name: crm_db_prod
    restart: unless-stopped
    environment:
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: crm
    volumes:
      - /opt/kfzblitz24/data/prod/crm-postgres:/var/lib/postgresql/data
    networks:
      - prod_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crm -d crm"]
      interval: 5s
      timeout: 5s
      retries: 10

  crm:
    build:
      context: .
      dockerfile: Dockerfile
    image: kfzblitz24/crm:prod
    container_name: crm_prod
    restart: unless-stopped
    depends_on:
      crm-db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://crm:${DB_PASSWORD}@crm-db:5432/crm
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_TRUST_HOST: "true"
      AUTH_URL: https://crm.kfzblitz24-group.com
      NEXTAUTH_URL: https://crm.kfzblitz24-group.com
      RESEND_API_KEY: ${RESEND_API_KEY}
      FROM_EMAIL: ${FROM_EMAIL}
      FROM_NAME: ${FROM_NAME}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_NAME: ${ADMIN_NAME:-Admin}
    networks:
      - prod_internal
      - traefik_net
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik_net
      - traefik.http.routers.crm-prod.rule=Host(`crm.kfzblitz24-group.com`)
      - traefik.http.routers.crm-prod.entrypoints=websecure
      - traefik.http.routers.crm-prod.tls=true
      - traefik.http.routers.crm-prod.tls.certresolver=letsencrypt
      - traefik.http.services.crm-prod.loadbalancer.server.port=3000

networks:
  prod_internal:
    external: true
  traefik_net:
    external: true
